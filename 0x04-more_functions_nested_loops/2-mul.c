#include "main.h"
#include <stdio.h>
/**
 * nul - multiplies two numbers
 * @a: the first integer
 * @b: the second integer
 * Return: Always 0
 */
int nul(int a, int b)
{
int result = a * b;
return (result);
}
