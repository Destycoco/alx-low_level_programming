#include "main.h"
/**
 * _abs - a function that computes the abs value of n
 *
 *
 * @r: the character  that is checked
 * Return: the abs of int
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
		return (-r);
}
